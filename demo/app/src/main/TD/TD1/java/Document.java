// This is an interface called Document which defines two methods that every document should implement.
public interface Document {
    // Method that returns the title of the document as a String.
    String titre();

    // Method that returns the total size of the document as an integer.
    int taille();
}
